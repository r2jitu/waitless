load("@rules_shell//shell:sh_binary.bzl", "sh_binary")

package(default_visibility = ["//visibility:public"])

exports_files([
    "run-local.sh",
    "make-limine-iso.sh",
    "helpers.sh",
])

sh_binary(
    name = "make_limine_iso",
    srcs = ["make-limine-iso.sh"],
    data = ["//boot:limine.conf"],
)

# The VZ.framework runner moved to //tools/run-vz:run_vz alongside the
# tools/hvf-runner crate. Update any references.
