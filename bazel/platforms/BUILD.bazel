package(default_visibility = ["//visibility:public"])

platform(
    name = "x86_64_unikernel",
    constraint_values = [
        "@platforms//cpu:x86_64",
        "@platforms//os:none",
    ],
)

platform(
    name = "aarch64_unikernel",
    constraint_values = [
        "@platforms//cpu:aarch64",
        "@platforms//os:none",
    ],
)

# config_setting helpers used in select() expressions across the repo
config_setting(
    name = "x86_64",
    constraint_values = ["@platforms//cpu:x86_64"],
)

config_setting(
    name = "aarch64",
    constraint_values = ["@platforms//cpu:aarch64"],
)

# Matches any unikernel platform (bare-metal, os=none).
# Used by tests to mark themselves as incompatible with the unikernel toolchain.
config_setting(
    name = "os_none",
    constraint_values = ["@platforms//os:none"],
)
